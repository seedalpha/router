box: wercker/nodejs
build:
  steps:
    - script:
        name: update registry config
        code: |-
          echo "$NPM_CONFIG" > .npmrc
          cat .npmrc
          npm install
          echo "$ZUUL_CONFIG" > ~/.zuulrc
          npm test